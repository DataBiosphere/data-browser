import { sumForExampleTest } from "../sumForExampleTest";

test("adds 1 + 2 to equal 3", () => {
  expect(sumForExampleTest(1, 2)).toBe(3);
});

/*
test("Adds 1+3 to equal -23", () => {
  expect(sumForExampleTest(1,3)).toBe(-23);
});*/
