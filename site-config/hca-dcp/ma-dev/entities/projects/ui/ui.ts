import { EntityUIConfig } from "@databiosphere/findable-ui/lib/config/entities";
import { SLOTS } from "./slots";

export const UI: EntityUIConfig = {
  enableExportButton: true,
  enableSummary: true,
  enableTabs: true,
  slots: SLOTS,
};
