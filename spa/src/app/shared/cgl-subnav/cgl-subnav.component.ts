// Dependencies
import { Component, Input } from "@angular/core";

/**
 * Subnav component, contains "page title" and additional actions (on right hand side).
 */

@Component({
    selector: "cgl-subnav",
    templateUrl: "cgl-subnav.component.html",
    styleUrls: ["cgl-subnav.component.scss"]
})

export class CGLSubnavComponent {
}