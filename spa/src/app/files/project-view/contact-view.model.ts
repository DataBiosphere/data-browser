/**
 * Human Cell Atlas
 * https://www.humancellatlas.org/
 *
 * Model of contact view, included in JSON response returned from project API.
 */

export interface ContactView {
    name: string;
    email: string;
    institution: string;
}
