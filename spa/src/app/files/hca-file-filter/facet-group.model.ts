/*
 * Human Cell Atlas
 * https://www.humancellatlas.org/
 *
 * Collection of facets that are grouped into a single drop down.
 */

export interface FacetGroup {
    facetGroupName: string;
    facetNames: string[];
}
