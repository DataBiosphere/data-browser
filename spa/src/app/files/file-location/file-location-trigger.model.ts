/**
 * Human Cell Atlas
 * https://www.humancellatlas.org/
 *
 * Possible set of triggers that initiate file location requests.
 */

export enum FileLocationTrigger {
    "COPY" = "COPY",
    "DOWNLOAD" = "DOWNLOAD"
}
