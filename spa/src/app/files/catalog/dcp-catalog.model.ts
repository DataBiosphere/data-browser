/**
 * Human Cell Atlas
 * https://www.humancellatlas.org/
 *
 * Subset of DCP catalogs, used by UI toggles to switch between dcp1 and dcp2 catalogs.
 */

export enum DCPCatalog {
    "DCP1" = "dcp1",
    "DCP2" = "dcp2"
}
