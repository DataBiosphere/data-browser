/**
 * Human Cell Atlas
 * https://www.humancellatlas.org/
 *
 * Term name types, used when sorting terms for display. Default type is string.
 */

export enum TermNameSortType {
    "AGE_UNIT" = "AGE_UNIT",
    "NUMBER_RANGE" = "NUMBER_RANGE",
}
